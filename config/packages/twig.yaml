twig:
    file_name_pattern: '*.twig'
    # Bootstrap 5 markup for expanded choices (checkbox rows); base div layout is merged automatically.
    form_themes:
        - 'bootstrap_5_layout.html.twig'

when@test:
    twig:
        strict_variables: true
